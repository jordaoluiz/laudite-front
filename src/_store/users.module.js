import { userService } from '../_services';

const state = {
    all: {}
};

const actions = {
};

const mutations = {
   
};

export const users = {
    namespaced: true,
    state,
    actions,
    mutations
};
